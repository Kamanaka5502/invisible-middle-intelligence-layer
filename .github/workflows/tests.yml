name: tests-disabled

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Disabled pending local validation
        run: echo "CI is disabled until the public runtime scaffold is locally validated."
